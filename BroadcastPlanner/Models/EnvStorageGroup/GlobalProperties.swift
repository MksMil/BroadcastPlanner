import Foundation

// global  properties, string constants, localizeble strings, network links

struct GlobalProperties {
    
}

enum ImagePath: String{
    case userImage
    case stadiumBackground
    case teamLogo
}
